# Dependencies & build output
node_modules/
.next/
*.tsbuildinfo

# Secrets — never committed (see docs/DROPLET_ACCESS.md)
.env
.env.*
!.env.example

# OS / editor noise
Thumbs.db
.DS_Store

# Local review copies (regenerated from data/)
~$*.xlsx

# Local Claude Code settings
**/.claude/settings.local.json

# Personal info — this repo is public on GitHub. Real name, work email,
# employer, and personal circumstances do not belong in it. Added
# 2026-07-19 after finding this file untracked and about to be pushed.
PERSONAL_PROFILE.md
